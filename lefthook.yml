pre-commit:
  parallel: false
  commands:
    oxfmt:
      glob: '*.{md,yml,js,mjs,ts}'
      exclude:
        - CLAUDE.md
        - .claude
      run: npx --no oxfmt {staged_files} && git add {staged_files}
